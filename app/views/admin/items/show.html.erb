<h2>商品詳細<h2>

<%= image_tag @item.image, size: '300x200' %>

  <h4>商品名<h4>
    <%= @item.name %>

  <h4>商品説明<h4>
    <%= @item.intro %>

  <h4>ジャンル<h4>
    <%= @item.genre.name %>

  <h4>税込価格（税抜価格）<h4>
    <%= (@item.tax_free_price*1.1).floor.to_s(:delimited) %>(<%= (@item.tax_free_price).floor.to_s(:delimited) %>)円

  <h4>販売ステータス<h4>
    <% if @item.sale_status == false %>
      <%= '販売中' %>
    <% else %>
      <%= '販売停止中' %>
    <% end %>

<%= link_to "編集する", edit_admin_item_path(@item.id) %>
