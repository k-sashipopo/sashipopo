<table class="table table-borderless table-hover text-center mt-3">
  <thead class="table">
    <tr>
      <th>ジャンル検索</th>
    </tr>
  </thead>
    <% genres.each do |genre| %>
    <tr>
      <td class="text-center rounded-pill">
        <%= link_to "#{genre.name}", items_path(genre_id: genre.id), class: "text-dark" %>
      </td>
    </tr>
    <% end %>
</table>
