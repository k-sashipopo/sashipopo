<div class="container-fluid">
  <div class="row mt-3">
    <div class="col text-center">
      <%= link_to "カートを空にする", destroy_all_cart_items_path, method: :delete, data: { confirm: "カートを空にしますか？" }, class: 'btn btn-outline-danger' %>
      
      <table class="table table-bordered contents-area mt-3">
        <thead class="">
          <tr>
            <th>商品名</th>
            <th>単価(税込)</th>
            <th>数量</th>
            <th>小計</th>
            <th></th>
          </tr>
            
          <tbody>
            <% @cart_items.each do |cart_item| %>
            <tr>
              <td><%= image_tag cart_item.item.image, size: "80x70" %>
                <p><%= cart_item.item.name %></p>
              </td>
              <td class="align-middle"> <!--税込価格-->
                <!--(cart_item.tax_free_price*1.1).floor.to_s(:delimited)-->
                <p><%= cart_item.with_tax_price %></p>
              </td>
              <td class="align-middle">
                <%= form_with model: cart_item, url: cart_item_path(cart_item.id), method: :patch do |f| %>
                  <%= f.select :count, *[1..10], class: "form-control" %>
                  <%= f.submit "変更", class: "btn btn-outline-success" %>
                <% end %>
              </td>
              <td class="align-middle"> <!--小計-->
                  <!--cart_item.item.tax_free_price*1.1).count.to_s(:delimited)-->
                  <%= cart_item.subtotal %>
                  </td>
              <td class="align-middle">
                <%= link_to "削除する", cart_item_path(cart_item), method: :delete, data: { confirm: "商品を削除しますか？", class: "btn btn-outline-danger" } %>
              </td>
            </tr>
            <% end %>
          </tbody>
        </thead>
      </table>
      
      <%= link_to "買い物を続ける", items_path, class: "btn btn-outline-primary m-2 px-3" %>
      
      <table class="table table-bordered contents-area col-3 ml-auto">
        <tbody>
          <tr>
            <th class="">合計金額</th>
            <!--<th>%= @total_price.to_s(:delimited) %></th>-->
            <!--<th>%= number_with_delimiter(@total_price) %></th>-->
            <th><%= @total_price %></th>
          </tr>
        </tbody>
      </table>
      
      <%= link_to "情報入力に進む", new_order_path(buy_quantity: @total_price), class: "btn btn-outline-success mb-3" %>
      
    </div>
  </div>
</div>

